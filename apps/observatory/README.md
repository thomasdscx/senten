# Senten Observatory

Observatory is the optional visual workbench for Senten. It will render the same canonical semantic/evidence objects used by the CLI and machine APIs; it will not implement a parallel analysis stack.

Planned views: StateTruss graph, environments/sandboxes, Interaction Assurance paths, policies, invariants, evidence, agents, runtime traces, semantic diffs, reports, and LaunchProof verification.
